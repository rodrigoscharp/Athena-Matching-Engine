# Allow ArchUnit rules to pass when no classes match the that() predicate.
# This is intentional in Sprint 1 — most packages are still empty.
# As production code is added in future sprints, rules will enforce against real classes.
# Remove this setting or set to true once all bounded contexts have production code.
archRule.failOnEmptyShould = false
